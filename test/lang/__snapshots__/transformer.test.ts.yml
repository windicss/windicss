Tools / simple syntax / js / 0: |-
  let number = 2;
  let a = number;
  let b = add(mul(10, a), div(mul(10, number), 4));
  let c = minus(a, negative(b));
  let testVariable = "this is a string";
  let template = `test hello ${add(a, b)} ${c}`;
